# Fixtures

Fixtures model governed repositories used by focused Python CLI tests.

Each fixture should be small, explicit, and scoped to one behaviour. Prefer adding a new fixture over expanding a shared fixture until the command surface is stable.
