# Maintainer documentation

This directory contains the small set of operational documents needed to
maintain the current Python CLI and its local SDF operating layer.

The source tree and tests remain the primary implementation reference.
