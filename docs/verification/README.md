# Verification

The repository-local verification boundary is `.sdf/verification.yml`.

Run `sdf verify --repo .` to execute the configured checks and use `sdf close`
for governed closeout. Focused checks are diagnostic evidence, not a substitute
for the configured boundary.
